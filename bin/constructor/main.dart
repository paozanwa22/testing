
import 'person.dart';
import 'student.dart';

void main(List<String> args) {
  // 1
  // Person p = Person(); //  buat object

  // 2
  // Person p = Person();
  // p.name = 'joko'; //inisialisasi/memberi nilai pada proferti name
  // print(p.name);

  // 3
  // kirim nilai parameter
  // Person p = Person('joko');
  // print(p.name);
  
  // 4
  // objek menerima optional parameter
  // Person p = Person(name: 'joko');
  // print(p.name); 

  // 5
  // Person s = Student();
  // print(s.name);

}